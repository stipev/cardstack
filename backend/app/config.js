const SECONDS = 1000;
const MINUTES = 60 * SECONDS;
const HOURS = 60 * MINUTES;
const DAYS = 24 * HOURS;

const REFRESH_RATE = 5;

module.exports = {
  SECONDS,
  MINUTES,
  DAYS,
  HOURS,
  REFRESH_RATE
};
